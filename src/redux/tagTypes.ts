export enum tagTypes {
  skill = "skill",
  project = "project",
  blog = "blog",
  contact = "contact",
}

export const tagTypesList = [tagTypes.skill, tagTypes.project, tagTypes.blog, tagTypes.contact];
